import { Disablerightclick } from './disablerightclick';

describe('Disablerightclick', () => {
  it('should create an instance', () => {
    const directive = new Disablerightclick();
    expect(directive).toBeTruthy();
  });
});
